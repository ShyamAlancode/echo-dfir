[pytest]
markers =
    spoliation: red-team evidence integrity tests
    integration: end-to-end tests requiring SIFT tools
    adversarial: adversarial AI and forensic accuracy tests
    slow: tests >5s
testpaths = tests
addopts = -ra --strict-markers --tb=short
